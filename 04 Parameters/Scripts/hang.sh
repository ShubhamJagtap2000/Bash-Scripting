#!/bin/bash

echo Enter your name

read name

echo "Your name is $name"

# Run this script inputting your name as "Alex" after running using command ./hang.sh in the terminal it will output result with the name you gave as input, otherwise it will hang
